{
  "name": "LiveTodoList",
  "version": "0.0.1",
  "description": "Live Todolist using WebSocket",
  "author": "Yoyote",
  "license": "ISC",
  "private": true,
  "main": "app.js",
  "scripts": {
    "start": "npm run start:dev",
    "start:dev": "nodemon app.js",
    "start:prod": "cross-env NODE_ENV=production node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cross-env": "^5.1.3",
    "express": "^4.16.2",
    "socket.io": "^2.0.4"
  },
  "devDependencies": {
    "nodemon": "^1.14.1",
    "browser-sync": "^2.20.0"
  }
}
